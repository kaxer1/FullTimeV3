import { DepartamentoPadrePipe } from './departamento-padre.pipe';

describe('DepartamentoPadrePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartamentoPadrePipe();
    expect(pipe).toBeTruthy();
  });
});
