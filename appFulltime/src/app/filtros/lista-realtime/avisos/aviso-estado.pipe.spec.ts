import { AvisoEstadoPipe } from './aviso-estado.pipe';

describe('AvisoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new AvisoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
