import { AvisoDescPipe } from './aviso-desc.pipe';

describe('AvisoDescPipe', () => {
  it('create an instance', () => {
    const pipe = new AvisoDescPipe();
    expect(pipe).toBeTruthy();
  });
});
