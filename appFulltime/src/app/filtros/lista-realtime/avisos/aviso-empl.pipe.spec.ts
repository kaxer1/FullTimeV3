import { AvisoEmplPipe } from './aviso-empl.pipe';

describe('AvisoEmplPipe', () => {
  it('create an instance', () => {
    const pipe = new AvisoEmplPipe();
    expect(pipe).toBeTruthy();
  });
});
