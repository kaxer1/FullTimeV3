import { EmplApellidoPipe } from './empl-apellido.pipe';

describe('EmplApellidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmplApellidoPipe();
    expect(pipe).toBeTruthy();
  });
});
