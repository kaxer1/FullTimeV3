import { EmplNombrePipe } from './empl-nombre.pipe';

describe('EmplNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new EmplNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
