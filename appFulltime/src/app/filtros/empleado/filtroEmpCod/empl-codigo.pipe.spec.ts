import { EmplCodigoPipe } from './empl-codigo.pipe';

describe('EmplCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmplCodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
