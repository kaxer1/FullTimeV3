import { TimbreEmpleadoPipe } from './timbre-empleado.pipe';

describe('TimbreEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TimbreEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
