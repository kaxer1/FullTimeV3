import { EmplCedulaPipe } from './empl-cedula.pipe';

describe('EmplCedulaPipe', () => {
  it('create an instance', () => {
    const pipe = new EmplCedulaPipe();
    expect(pipe).toBeTruthy();
  });
});
