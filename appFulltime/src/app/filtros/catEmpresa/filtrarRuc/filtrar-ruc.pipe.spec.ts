import { FiltrarRucPipe } from './filtrar-ruc.pipe';

describe('FiltrarRucPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarRucPipe();
    expect(pipe).toBeTruthy();
  });
});
