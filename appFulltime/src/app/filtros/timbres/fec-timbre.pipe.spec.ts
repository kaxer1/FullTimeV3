import { FecTimbrePipe } from './fec-timbre.pipe';

describe('FecTimbrePipe', () => {
  it('create an instance', () => {
    const pipe = new FecTimbrePipe();
    expect(pipe).toBeTruthy();
  });
});
