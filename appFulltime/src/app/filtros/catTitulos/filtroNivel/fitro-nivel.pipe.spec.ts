import { FitroNivelPipe } from './fitro-nivel.pipe';

describe('FitroNivelPipe', () => {
  it('create an instance', () => {
    const pipe = new FitroNivelPipe();
    expect(pipe).toBeTruthy();
  });
});
