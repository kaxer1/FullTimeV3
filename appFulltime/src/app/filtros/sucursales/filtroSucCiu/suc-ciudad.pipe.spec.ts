import { SucCiudadPipe } from './suc-ciudad.pipe';

describe('SucCiudadPipe', () => {
  it('create an instance', () => {
    const pipe = new SucCiudadPipe();
    expect(pipe).toBeTruthy();
  });
});
