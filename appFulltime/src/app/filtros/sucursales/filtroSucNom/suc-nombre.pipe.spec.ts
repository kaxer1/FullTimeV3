import { SucNombrePipe } from './suc-nombre.pipe';

describe('SucNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new SucNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
