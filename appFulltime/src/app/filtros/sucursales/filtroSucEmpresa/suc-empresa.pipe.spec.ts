import { SucEmpresaPipe } from './suc-empresa.pipe';

describe('SucEmpresaPipe', () => {
  it('create an instance', () => {
    const pipe = new SucEmpresaPipe();
    expect(pipe).toBeTruthy();
  });
});
