import { FiltroDepartamentoRPipe } from './filtro-departamento-r.pipe';

describe('FiltroDepartamentoRPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDepartamentoRPipe();
    expect(pipe).toBeTruthy();
  });
});
