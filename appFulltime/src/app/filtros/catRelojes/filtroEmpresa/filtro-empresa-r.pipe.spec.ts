import { FiltroEmpresaRPipe } from './filtro-empresa-r.pipe';

describe('FiltroEmpresaRPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmpresaRPipe();
    expect(pipe).toBeTruthy();
  });
});
