import { FiltrosNombresPipe } from './filtros-nombres.pipe';

describe('FiltrosNombresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosNombresPipe();
    expect(pipe).toBeTruthy();
  });
});
