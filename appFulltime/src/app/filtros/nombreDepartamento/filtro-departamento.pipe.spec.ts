import { FiltroDepartamentoPipe } from './filtro-departamento.pipe';

describe('FiltroDepartamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDepartamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
