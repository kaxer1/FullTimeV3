import { EnrNombrePipe } from './enr-nombre.pipe';

describe('EnrNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new EnrNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
