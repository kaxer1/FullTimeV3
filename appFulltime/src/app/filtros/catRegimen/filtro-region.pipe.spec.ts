import { FiltroRegionPipe } from './filtro-region.pipe';

describe('FiltroRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
